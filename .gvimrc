set ts=4
if has("gui_running")
	if has("gui_win32")
		set guifont=Ubuntu_Mono:h11:cANSI
	else
		set guifont=Ubuntu\ Mono\ 11
	endif
endif
set autoindent
set number
set ruler
set nowrap
set lines=40
set columns=100
